import 'package:flutter/material.dart';

class Another extends StatelessWidget {
  static String path = "/another";
  const Another({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
